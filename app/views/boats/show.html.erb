<div class="container margin-top-negative">

    <div class="row">
      <div class="col-8">
        <div class="flat-show-card">
          <%= cl_image_tag @boat.photo.key, height: 300, width: 400, crop: :fill if @boat.photo.attached? %>

          <h3><%= @boat.category %></h3>
          <h5><%= @boat.name %></h5>
          <p class="flat-show-address"><%= @boat.address %></p>
        </div>
        <div class="flat-show-card">
          <h4>A bit more about this boat...</h4>
          <p><%= @boat.description %></p>
<div class="card">
  <div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
</div>
        </div>
      </div>

      <div class="col-4">

<div class="flat-show-card">
  <h5>Book this place!</h5>

  <div data-controller="toggle" class="mt-5">
    <div data-toggle-target="togglableElement" class="d-none">
      <%= simple_form_for([@boat, @reservation]) do |b| %>
        <div class="input-group">
          <h1> <%= b.input :name %> </h1>
          <h1> <%= b.input :last_name %> </h1>
          <h1> <%= b.input :email %> </h1>
          <span class="input-group-text" id="start-date-label">From:</span>
          <%= b.input :start_date,
                      as: :string,
                      input_html: { data: { controller: "datepicker" } } %>
          <%= b.input :end_date,
                      as: :string,
                      input_html: { data: { controller: "datepicker" } } %>
          <%= b.submit class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>

    <input data-action="click->toggle#fire" type="submit" value="Book&Reserv" class="btn btn-success mt-2" style="width: 100%">
  </div>
</div>


          <input data-action="click->toggle#fire" type="submit" value="Book&Reserv" class="btn btn-success mt-2" style="width: 100%">

          </form>
          </div>
          </div>
        </div>
        <div class="flat-show-card">
          <h3>Our Boat to yours Dream</h3>

          <div class="flex">

            <div class="flex ms-3">
              <%= @boat.price %>€
            </div>
          </div>
        </div>
        <div class="flat-show-card">

          <div class="flex-align">
            <h6 class="no-mb">Share</h6>

            <div class="flex">
              <i class="fa-brands fa-facebook-square me-2"></i>
              <i class="fa-brands fa-twitter-square me-2"></i>
              <i class="fa-brands fa-pinterest-square me-2"></i>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>
